<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="VirtualController.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VirtualController"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
  <Page.Resources>
    <local:BooleanToObjectConverter x:Key="b2RecordStop" TrueValue="Stop" FalseValue="Record" />
  </Page.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    <ContentControl Grid.Row="0" Content="{Binding Controller.Canvas}" Width="300" Height="400" />
    <Button Grid.Row="1" Content="Open Virtual Controller" Click="OpenVirtualController_Click" HorizontalAlignment="Center" />
    <Button IsEnabled="{Binding CanRecord}" Content="{Binding IsRecording, Converter={StaticResource b2RecordStop}}" 
            Grid.Row="2" Click="ToggleRecording_Click" HorizontalAlignment="Center" />
    <TextBlock Grid.Row="3" Text="{Binding LastErrorText}" />
  </Grid>
</Page>
